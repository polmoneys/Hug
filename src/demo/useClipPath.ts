import { LooseAutocomplete } from '../Hug/interfaces';

interface Props {
    variant: LooseAutocomplete<'circle' | 'rounded'>;
}
export default function useClipPath(props: Props) {
    const { variant } = props;

    return {};
}
